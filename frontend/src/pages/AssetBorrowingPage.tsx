import { PlaceholderModule } from "../components/PlaceholderModule";

export function AssetBorrowingPage() {
  return (
    <PlaceholderModule
      title="ระบบยืมอุปกรณ์"
      subtitle="โครงหน้าสำหรับการยืมและคืนอุปกรณ์"
      items={["รายการขอยืม", "ติดตามการคืน", "ประวัติการยืม"]}
    />
  );
}
